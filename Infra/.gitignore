# Local .terraform directories (providers, plugin cache, etc.)
**/.terraform/

# Terraform state files (local state and backups)
*.tfstate
*.tfstate.*

# Lock/coordination file
.terraform.tfstate.lock.info
terraform.tfstate.d/

# Terraform crash logs
crash.log
crash.*.log

# Sensitive variable files
*.tfvars
*.tfvars.json

# Plan output files
*.tfplan

# Local override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Optional: ignore Terraform CLI config if present locally
.terraformrc
terraform.rc
